export default function RoomSettingsSection({ muted, pinned, disabled = false, onMute, onPin, onClear, onExit }) {
  const button = "flex w-full items-center justify-between rounded-xl px-3 py-2.5 text-left text-[13px] hover:bg-black/[0.035] dark:hover:bg-white/[0.05]";
  return <div className="space-y-1"><button type="button" disabled={disabled} className={`${button} disabled:opacity-50`} onClick={onMute}><span>Notifications</span><span className="text-[11px] text-[#8C9198]">{muted ? "Muted" : "On"}</span></button><button type="button" disabled={disabled} className={`${button} disabled:opacity-50`} onClick={onPin}><span>{pinned ? "Unpin room" : "Pin room"}</span></button><button type="button" disabled={disabled} className={`${button} disabled:opacity-50`} onClick={onClear}><span>Clear chat for me</span></button><button type="button" disabled={disabled} className={`${button} text-[#B65A5A] disabled:opacity-50 dark:text-[#E39A9A]`} onClick={onExit}><span>Exit group</span></button></div>;
}
